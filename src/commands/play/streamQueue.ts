import { VoiceConnection, StreamDispatcher } from 'discord.js'
import { Readable } from 'stream'
import Queue from 'double-ended-queue'

// const queue: Queue<StreamDispatcher> = new Queue()

// export function play(conn: VoiceConnection, resource: string | Readable) {
//   conn.
//   queue.enqueue(conn.play(resource))

//   if (conn.voice.speaking) {
//     log.debug("Enqueueing")
//   }

// }

// function playNext() {}
